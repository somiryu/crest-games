using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MyCollection : MonoBehaviour
{
    MonsterMarketManager monsterMarketManager => MonsterMarketManager.Instance;
    public List<MonsterChestType> collectedMonsters = new List<MonsterChestType>();
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
